#ifndef TREESEDITOR_HPP
#define TREESEDITOR_HPP

#include <QTreeView>
#include <QStandardItemModel>

/*class TreesEditor
{
public:
    TreesEditor();

    void update(const QStringList &items, int index);
    QVector<QTreeView*> get() const noexcept;
    QTreeView* current(int index);

private:
    QVector<QTreeView*> treeViews_;
    QVector<QStandardItemModel*> models_;
};*/

#endif // TREESEDITOR_HPP
