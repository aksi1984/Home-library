#include "treeseditor.hpp"

/*TreesEditor::TreesEditor()
{
    for(int i = 0; i < 2; ++i)
    {
        QTreeView* tree = new QTreeView;
        QStandardItemModel* model = new QStandardItemModel;
        tree->setModel(model);
        models_.push_back(model);
        treeViews_.push_back(tree);

    }
}

void TreesEditor::update(const QStringList &items, int index)
{
    models_[index]->setItem(0, 0, new QStandardItem("Main"));
}

QVector<QTreeView*> TreesEditor::get() const noexcept
{
    return treeViews_;
}

QTreeView* TreesEditor::current(int index)
{
    return treeViews_[index];
}*/
